language: python
dist: xenial
python:
  - "3.7"
install: pip install -r requirements.txt
script:
  - python -Wall -m unittest discover -t . -s evtstrd_test -p "*.py"
  - mypy evtstrd evtstrd_test
  - flake8
